// toggle between adding and removing .active class to fa-bars (hamburger menu icon)
$(".fa-bars").click(function() {
	  $(".hamburger-menu").toggleClass("active");
  });

// toggle between adding and removing .active class to fa-times (X icon)
$(".fa-times").click(function() {
	  $(".hamburger-menu").toggleClass("active");
  });
